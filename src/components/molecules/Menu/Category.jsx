import React from "react"

const Category = (props) => {
    return (
        <div>
            <img src="#"/>
            <a>{props.nameOfCategory}</a>
        </div>
    );
};

export default Category;